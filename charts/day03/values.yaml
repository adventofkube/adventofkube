namespace: day03
appName: flag-server

# Pod label uses "myapp" (no hyphen)
podLabel: myapp

# Service selector uses "my-app" (with hyphen) — intentional mismatch!
serviceSelector: my-app

image: ghcr.io/adventofkube/day03:v0.1.0
